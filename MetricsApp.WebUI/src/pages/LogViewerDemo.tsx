import React from 'react';
import LogViewer from './LogViewer';

// Demo component that shows the LogViewer with sample data
const LogViewerDemo: React.FC = () => {
  return (
    <div className="h-screen">
      <LogViewer />
    </div>
  );
};

export default LogViewerDemo;
